<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.digitaltechnologyassessmentterm4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DigitalTechnologyAssessmentTerm4">
        <activity android:name=".play_screen"></activity>
        <activity android:name=".win_screen" />
        <activity android:name=".lose_screen" />
        <activity android:name=".song_select_screen" />
        <activity android:name=".customise_screen" />
        <activity android:name=".shop_screen" />
        <activity android:name=".settings_screen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>